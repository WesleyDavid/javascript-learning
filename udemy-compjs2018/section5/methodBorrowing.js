// Make one object with a method

var dice = {
 currentRoll: 0,
 lastRoll: 0,
 roll: function () {
  return "You just rolled!";
 }
};

// Make another object without

// borrow with =

// Then achieve the same functionality with .call
